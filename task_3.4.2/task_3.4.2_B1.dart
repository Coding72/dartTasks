void main(List<String> arguments) {
  double orderValue; //(Bestellwert in Euro als Dezimalzahl)
  double distance = 0; // (Entfernung in km als Dezimalzahl) 
  bool rushHour; // (Boolean für Hauptverkehrszeit)
  bool isRaining; // (Boolean für aktuelles Regenwetter)
  int orderSize; // (Anzahl der Artikel als Ganzzahl)

  if (distance <= 5){
  } else if (distance > 5 && distance<=10){
  } else if (distance>10){}
 

}

